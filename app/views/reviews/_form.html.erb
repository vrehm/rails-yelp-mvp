<h1>Add a review</h1>
<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.error_notification %>

  <%= f.input :content %>
  <%= f.input :rating, collection: 0..5, prompt: "Nb of stars", selected: 3 %>

  <%= f.button :submit %>
<% end %>
